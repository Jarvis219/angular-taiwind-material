import { TransformStatusCartPipe } from './transform-status-cart.pipe';

describe('TransformStatusCartPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformStatusCartPipe();
    expect(pipe).toBeTruthy();
  });
});
