import { TransformPaymentPipe } from './transform-payment.pipe';

describe('TransformPaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformPaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
